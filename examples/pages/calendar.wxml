<!--pages/calendar.wxml-->
<view class="page-wrap">
  <calendar is-time-show="{{isTimeShow}}" time-gap="{{timeGap}}" bind:change="timeChange" />

  <button bindtap="showCalendar">显示日历</button>
</view>
